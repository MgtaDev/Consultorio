import { createContext } from 'react'

const UserContext = createContext({}) // Crea el contexto User con un objeto vacío como valor por defecto

export default UserContext // Exporta el contexto User para ser importado en otros archivos